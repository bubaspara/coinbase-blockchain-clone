import React, { useState } from "react";
import styled from "styled-components";
const CoinSelector = ({
  setAction,
  selectedToken,
  setSelectedToken,
  sanityTokens,
  thirdWebTokens,
  walletAddress,
}) => {
  return <Wrapper>CoinSelector</Wrapper>;
};

export default CoinSelector;

const Wrapper = styled.div``;
